# 2026-07-14 작업 목표

> 마감: 오후 5:40

## 오늘 목표

- [x] 정찰(Patrol) 상태 완성 — Wait → 랜덤 회전 → 걷기 반복
- [x] 경계(Alert) 상태 완성 — 감지 시 접근 + 포효 1회 → 전투 전이
- [x] 전투(Combat) 루프 골격 — 타겟 랜덤 선정 → 콤보 1~3회 → (Roar로 임시 검증) 정상 동작 확인
- [x] **몬스터 스탯 부여하기** — 체력(500), 그로기 수치(0~100) — `MonsterAttributeSet`/`MonsterCharacterBase` C++ 완성, GAS 디버거로 확인 완료
- [x] **몬스터 플레이어 기준 회전 만들기** — 단, 정식 포지셔닝(104~108)이 아니라 **테스트용 단순 버전** (`Event Tick`에서 `Find Look at Rotation` + `RInterpTo`로 Yaw만 타겟 쪽으로 부드럽게 회전). 팀원과 방향 논의 후 정식 포지셔닝 시스템으로 교체 예정.
- [ ] **몬스터 공격에 GAS 시스템 붙이기** — 마무리 직전 (아래 참고)
- [ ] **몬스터 공격 패턴 3가지 이상 만들기** — 아직 시작 안 함, 현재 Roar 임시 대체 중

## 진행 상황 / 막힌 지점

- GAS 데미지 적용 버그 2개 발견/수정:
  1. `FGameplayEffectSpecHandle`은 구조체라서 `Make Outgoing Spec` 출력에서 `Assign Tag Set by Caller Magnitude`와 `Apply GameplayEffect Spec To Target`로 와이어를 각각 분기하면 서로 다른 복사본이 되어 매그니튜드가 반영 안 됨 → **로컬 변수로 승격 후 Get으로만 넘기도록 수정**
  2. `GE_DamageEffect`는 `Op=Add`라서 Health를 줄이려면 Magnitude를 **음수**로 넘겨야 함 (양수로 넘기면 이미 MaxHealth라 클램프되어 안 줄어드는 것처럼 보임) → `Damage × -1.0` 추가해서 수정
  - 그로기는 원래 양수라 먼저 정상 동작 확인됨. 데미지 쪽도 위 수정 반영했으나 **HP 실제 감소 여부는 아직 테스트 안 함**
- 아키텍처 논의: 플레이어처럼 몬스터도 자기 BP(`BP_Alatreon`)에 `Take Damage` 커스텀 이벤트를 두고, `COL_HitBox`는 그 이벤트만 호출하도록 정리하기로 함 (지금은 `COL_HitBox` 안에서 직접 처리 중) — 아직 이관 안 함
- (별개 작업) 팀원 git 병합 사고 복구: `Content/NJW` + 전체 `Content` 폴더를 팀원 최신본과 비교해서 LDJ 폴더 등 누락/구버전 파일 다수 복원 완료. `.gitignore`의 `!*.md` 규칙이 `CLAUDE.md` 무시 설정을 덮어쓰던 문제도 수정 완료.

## 오늘 안 끝나면 다음 세션에 이어할 것

1. **GAS 데미지 최종 확인**: 수정한 `Take Damage` 그래프로 플레이해서 몬스터 HP가 실제로 줄어드는지 확인
2. 확인되면 `Take Damage` 로직을 `COL_HitBox`에서 `BP_Alatreon`(또는 `MonsterCharacterBase`)으로 이관 — `Ability System Component` 입력 핀만 `Get Ability System Component`(Self)로 교체, `COL_HitBox`는 캐스트 후 이벤트 호출만 하도록 단순화
3. **공격 패턴 3가지 이상** 만들기 — 우선 기존 애니메이션으로 임시 패턴 3개를 `ComboStep`에 랜덤 분기로 연결해서 요건부터 채우기 (디자인 문서의 DataTable 방식은 추후 정식 적용)
4. 팀원과 논의 후 방향 정해지면, 지금 넣은 단순 회전을 정식 포지셔닝 시스템(Animation 104/105/106/107/108, `FAlatreonPatternRow`의 `RequiresRearTarget`/`CanHitFromRear` 활용)으로 교체
5. (미루기로 한 것, 참고용) 소경직/쓰러짐 애니메이션 반응(`OnGroggyFlinch`/`OnGroggyDown`) 연결, Phase 2 전환 로직
